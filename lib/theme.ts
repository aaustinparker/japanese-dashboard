import { createTheme } from '@mui/material/styles';
import { purple } from '@mui/material/colors';

// Create a global theme that components will use
export const theme = createTheme({
    /*
  palette: {
    primary: {
      main: purple[800],
    },
    secondary: {
      main: purple[200],
    },
  },
    */
});
